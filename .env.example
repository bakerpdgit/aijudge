# OpenAI API Key
# Get your API key from https://platform.openai.com/api-keys
# Note: This app accepts the API key through the UI for security
# You can also use environment variables, but it's not recommended for client-side apps
VITE_OPENAI_API_KEY=your_api_key_here
